#!/bin/bash

PLATFORM=${1:-arm64}

if docker ps --all | grep rv-ubuntu > /dev/null; then
  set -v
  docker start -a "rv-ubuntu-$PLATFORM"
else
  set -v
  docker run -it --name "rv-ubuntu-$PLATFORM" --volume .:/src --platform "linux/$PLATFORM" homebrew/ubuntu22.04
fi
